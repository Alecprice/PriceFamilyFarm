/** @type {import('next').NextConfig} */
const nextConfig = {
  // Image optimization is on. This works automatically on Vercel.
  // If self-hosting elsewhere (not Vercel), install `sharp` for full
  // server-side optimization: npm install sharp
};

export default nextConfig;
