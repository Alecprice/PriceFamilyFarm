{
  "name": "price-family-farm-cloud-sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --config ./vitest.config.mjs",
    "bundle:check": "wrangler deploy --dry-run --outdir .wrangler/preflight",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.1"
  },
  "devDependencies": {
    "vitest": "^3.2.4",
    "wrangler": "^4.31.0"
  }
}
